---
name: ai-model-registry
description: 'Use whenever changing or reviewing AI provider models, model IDs, provider parameters, request payloads, configuration controls, defaults, options, pricing, compatibility, SDK surfaces, or AI Model Registry data and documentation. Registry data and code must stay synchronized, and registry commands must run inside Docker.'
---

# AI Model Registry

Before running any command, follow the Docker-only command rule in `AGENTS.md` and `documentation/development-workflow/AGENT-SKILLS.md`.

Read and follow `services/ai-model-registry/documentation/AI-MODEL-REGISTRY.md`. It defines the required synchronization contract and container-only registry workflow.
